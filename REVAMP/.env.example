# ── AI WAR ROOM · server-side configuration (Netlify environment) ─────────
# Optional. Without it, speed / latency / generation-time / TTS / STT-quality
# columns show N/A. Get a key at https://artificialanalysis.ai (free tier
# requires attribution, which the UI shows wherever these numbers appear).
ARTIFICIAL_ANALYSIS_API_KEY=

# Never put generation-provider keys here unless you intend to enable paid
# "Run live battle" calls; the current build reports liveBattle:false and
# performs no paid generation.
